<% content_for :title, "New question" %>

<div class="min-h-screen w-full flex items-center justify-center p-4">
  <div class="w-full max-w-lg bg-white p-6 sm:p-8">
    <h1 class="font-bold text-3xl sm:text-4xl text-slate-900 mb-6 text-center">New question</h1>

    <%= render "form", question: @question %>
  </div>
</div>

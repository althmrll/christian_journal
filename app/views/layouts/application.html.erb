<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Christian Journal" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Christian Journal">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <header class="bg-slate-800 border-b border-gray-200 py-4 px-5 shadow-sm">
    <div class="flex justify-between">
      <div class="container mx-2 font-bold text-4xl text-white">
        Christian Journal
      </div>
      <div>
        <nav class="flex items-center space-x-4 p-4 whitespace-nowrap">
          <%= link_to "Home", root_path, class: "text-white  hover:text-sky-400" %>
          <%= link_to "Entries", entries_path, class: "text-white hover:text-sky-400" %>
          <%= link_to "Prayers", prayers_path, class: "text-white hover:text-sky-400" %>
          <%= link_to "Questions", questions_path, class: "text-white hover:text-sky-400" %>
          <% if session[:authenticated] %>
             <%= button_to "Log Out", logout_path, method: :delete, class:"text-[#ff413b] font-semibold cursor-pointer hover:text-[#ff6b66]"%>
          <% end %>
        </nav>
      </div>
    </div>
  </header>
  <body>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
